# Element theme ui

# Frontend: react - vite

# intructions
lets build a reusable components,
we can use classes,
but it needs to be easy to use,
it needs to be reusable,
they needs to be easy to enhance,
each folder will contain all files for the element,

variant will be in enums,

lets use scss,


install all needed with npm,

use BEM,

remember it needs to be easy to implement,


build me a page that i can see examples for and always add there,

the base element needs to have all the fimilar base code for an certain component,
other needs to extend it

the main goal is to build a personal ui library, upload it to repo and install in my future projects using npm and use them,

